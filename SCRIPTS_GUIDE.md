# 脚本使用指南

## 📁 当前脚本列表

### 🔧 核心脚本
1. **`init_db.py`** - 数据库初始化和机体数据管理
2. **`scripts/scrape_wiki_data.py`** - 爬取Wiki技术参数
3. **`update_wiki_urls.py`** - 更新机体图片URL

### 🖼️ 图片管理脚本
4. **`scripts/add_image_fields.py`** - 添加多图片字段到数据库
5. **`scripts/manage_images.py`** - 多图片系统管理
6. **`scripts/upload_images.py`** - 图片上传工具

### 🛠️ 辅助工具
7. **`scripts/list_gundams.py`** - 查看数据库状态
8. **`scripts/delete_gundam.py`** - 删除机体工具

## 🚀 标准操作流程

### 1. 添加新机体
```bash
# 编辑 init_db.py，添加机体基础信息
# 然后运行初始化
python init_db.py
```

### 2. 爬取数据
```bash
# 爬取技术参数和描述
python scripts/scrape_wiki_data.py

# 更新图片URL
python update_wiki_urls.py
```

### 3. 设置多图片系统（可选）
```bash
# 添加图片字段
python scripts/add_image_fields.py

# 上传本地图片
python scripts/upload_images.py

# 配置图片URL
python scripts/manage_images.py
```

### 4. 查看状态
```bash
# 查看数据库状态
python scripts/list_gundams.py
```

## 📝 修改机体简介

### 方法1: 后端修改（推荐）
在 `init_db.py` 中找到对应机体，修改 `description` 字段：

```python
{
    'name': 'RX-93ν高达',
    'series': 'UC纪元',
    'year': '0093',
    'pilot': '阿姆罗·雷',
    'description': '你的新简介内容...',  # 修改这里
}
```

然后重新运行：
```bash
python init_db.py
```

### 方法2: 前端显示位置
简介在详情页模板 `app/templates/detail.html` 第121行显示：
```html
<p class="lead">{{ gundam.description|truncate(150) }}</p>
```

## 🎯 你的下一步操作

根据你的描述，正确的操作顺序是：

1. **在 `init_db.py` 中更新机体信息**
   - 修改 `description` 字段添加简介
   - 添加新机体数据

2. **运行数据库初始化**
   ```bash
   python init_db.py
   ```

3. **手动更新 `update_wiki_urls.py`**
   - 为新机体添加图片URL配置

4. **运行爬虫获取技术参数**
   ```bash
   python scripts/scrape_wiki_data.py
   ```

5. **运行图片URL更新**
   ```bash
   python update_wiki_urls.py
   ```

6. **准备和上传背景图片**
   ```bash
   python scripts/upload_images.py
   ```

## ⚠️ 注意事项

- `init_db.py` 是主要的数据管理文件，所有机体基础信息都在这里
- 爬虫脚本可以获取技术参数，但图片需要手动配置URL
- 背景图片需要本地准备并上传到对应目录
- 每次修改 `init_db.py` 后都需要重新运行初始化 